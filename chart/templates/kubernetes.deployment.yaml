apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes
  labels:
    app: krateo.io
    component: kubernetes
spec:
  replicas: 1
  selector:
    matchLabels:
      component: kubernetes
  template:
    metadata:
      labels:
        component: kubernetes
    spec:
      containers:
        - name: kubernetes
          image: {{ .Values.registry.images.kubernetes }}
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /ping
              port: 8080
            timeoutSeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /ping
              port: 8080
            timeoutSeconds: 5
            periodSeconds: 30
          envFrom:
            - configMapRef:
                name: krateo-uri-config
          env:
            - name: 'LOG_LEVEL'
              value: {{ .Values.log.level }}
      imagePullSecrets:
        - name: registry-credentials
